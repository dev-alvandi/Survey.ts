import styled from 'styled-components';

const LoadingCompletePosts = () => {
  return (
    <Container>
      <div></div>
    </Container>
  );
};

export default LoadingCompletePosts;

const Container = styled.div``;
